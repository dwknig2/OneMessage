# AGENTS.md

## Cursor Cloud specific instructions

This repository ("OneMessage") is currently an empty scaffold containing only a `README.md` and a `LICENSE` (CC0 1.0). There is no source code, no dependency manifest, no build system, and no runnable application.

- **No services to start.** No backend, frontend, or database exists yet.
- **No lint, test, or build commands.** No tooling is configured.
- **No package manager lockfile.** When code is added, match the lockfile to the correct package manager (e.g., `pnpm-lock.yaml` → pnpm, `package-lock.json` → npm).

Once source code is added, update this section with concrete instructions for installing dependencies, running the dev server, linting, testing, and building.
